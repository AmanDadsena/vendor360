name: vendor360_core
description: >-
  Vendor360 domain model and repository contracts, shared by the vendor app
  and any future lender or distributor client.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.13.0

# Deliberately dependency-free at runtime, following the carryo_core rule:
# this is the layer every client agrees on, and anything it imports becomes
# something every client is forced to accept.
dependencies:

dev_dependencies:
  test: ^1.25.0
  lints: ^6.0.0
